<!DOCTYPE html>
<html>
<head>
    <style>
        .purchase-container {
            width: 600px;
            padding: 20px;
            background-color: white;
            border: 2px solid black;
            border-radius: 10px;
            margin: 20px;
        }

        .purchase-box {
            width: 90%;
            padding: 15px;
            background-color: white;
            border: 2px solid black;
            border-radius: 10px;
            margin-bottom: 10px;
        }

        .purchase-info {
            margin-bottom: 10px;
        }

        .pending-checkbox {
            margin-left: 10px;
        }
    </style>
</head>
<body>

    <div class="purchase-container">
        <h2>Histórico de Compras</h2>

 <!--@*       @foreach (var compra in Model.HistoricoCompras)
        {
            <div class="purchase-box">
                <div class="purchase-info">
                    <div><strong>Vendedor:</strong> @venda.vendedor.handle</div>
                    <div><strong>Título da Sala:</strong> @sala.titulo</div>
                    <div><strong>Preço:</strong> @venda.Preco.ToString("C")</div>
                    <div><strong>Data da Compra:</strong> @venda.date.ToString("dd/MM/yyyy")</div>
                </div>

                <div>
                    <strong>Estado:</strong>
                    @if (venda.verified == "pendente")
                    {
                        <input type="checkbox" class="pending-checkbox" />
                        <span>Pendente</span>
                    }
                    else
                    {
                        <span>Entregue</span>
                    }
                </div>
            </div>
        }
    </div>*@-->

</body>
</html>